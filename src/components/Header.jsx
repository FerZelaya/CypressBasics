import React from "react";

const Header = () => {
  return (
    <div className="p-3 text-white bg-primary border border-primary rounded-3">
      <span className="fw-bold fs-2 text-uppercase"> React Cypress Demo</span>
    </div>
  );
};

export default Header;
